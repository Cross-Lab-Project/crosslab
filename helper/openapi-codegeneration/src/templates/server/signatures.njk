{%- import "../general/utils.njk" as utils -%}

{%- macro generateSignature(function) -%}
export type {{ function.signature }} = (
    {{- 
    ("parameters: " + function.name + "ParametersType" if function.parameters) +
    (", " if function.parameters and (function.body.content[0].schema.typeDeclaration or function.isProxyFunction or (function.security != "none" and ("jwt" in function.security)))) +
    ("body: " + function.name + "BodyType" if function.body.content[0].schema.typeDeclaration or function.isProxyFunction) +
    (", " if (function.body.content[0].schema.typeDeclaration or function.isProxyFunction) and function.security != "none" and ("jwt" in function.security)) +
    ("user: UserType" if function.security != "none" and ("jwt" in function.security))
    -}} 
    ) => Promise<{{ function.name }}ResponseType> | {{ function.name }}ResponseType
{%- endmacro -%}

/**
 * This file was automatically generated by openapi-codegeneration.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source OpenAPI file,
 * and run openapi-codegeneration to regenerate this file.
 */
import {
    {{- "\n\tUserType," if secured }}
    {{ typeDependencies | join(",\n\t") }}
} from "../types"
{% for function in functions -%}
{{ utils.generateInterfaces(function) }}
{{ generateSignature(function) }}
{% endfor -%}