/**
 * This file was automatically generated by openapi-codegeneration.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source OpenAPI file,
 * and run openapi-codegeneration to regenerate this file.
 */
import * as BasicValidation from './basicValidation.cjs';
import * as Signatures from './signatures.js';
import { FunctionWithErrors, ResponseData } from './types.js';

/**
 * This function validates the inputs to listBookings()
 */
export function validateListBookingsInput() {
  return true;
}

/**
 * This function validates the outputs of listBookings()
 */
export function validateListBookingsOutput(
  response: ResponseData,
): response is Signatures.ListBookingsResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 200) {
    if (!BasicValidation.validateListBookingsResponse200Response(response.body)) {
      (validateListBookingsOutput as FunctionWithErrors).errors = (
        BasicValidation.validateListBookingsResponse200Response as FunctionWithErrors
      ).errors;
      return false;
    }

    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateListBookingsOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}

/**
 * This function validates the inputs to createBooking()
 */
export function validateCreateBookingInput(
  parameters: Signatures.CreateBookingParameters,
  body: Signatures.CreateBookingBody,
) {
  if (parameters) {
    if (
      !BasicValidation.validateChangedUrlRequest(parameters['changedUrl']) &&
      parameters['changedUrl'] !== undefined
    ) {
      (validateCreateBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateChangedUrlRequest as FunctionWithErrors
      ).errors;
      return false;
    }
    if (
      !BasicValidation.validateDeletedUrlRequest(parameters['deletedUrl']) &&
      parameters['deletedUrl'] !== undefined
    ) {
      (validateCreateBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateDeletedUrlRequest as FunctionWithErrors
      ).errors;
      return false;
    }
  } else {
    if (parameters !== undefined) {
      return false;
    }
  }

  if (!BasicValidation.validateCreateBookingBodyRequest(body)) {
    (validateCreateBookingInput as FunctionWithErrors).errors = (
      BasicValidation.validateCreateBookingBodyRequest as FunctionWithErrors
    ).errors;
    return false;
  }

  return true;
}

/**
 * This function validates the outputs of createBooking()
 */
export function validateCreateBookingOutput(
  response: ResponseData,
): response is Signatures.CreateBookingResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 201) {
    if (!BasicValidation.validateCreateBookingResponse201Response(response.body)) {
      (validateCreateBookingOutput as FunctionWithErrors).errors = (
        BasicValidation.validateCreateBookingResponse201Response as FunctionWithErrors
      ).errors;
      return false;
    }

    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateCreateBookingOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}

/**
 * This function validates the inputs to getBooking()
 */
export function validateGetBookingInput(parameters: Signatures.GetBookingParameters) {
  if (parameters) {
    if (!BasicValidation.validateBookingIdRequest(parameters['bookingId'])) {
      (validateGetBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateBookingIdRequest as FunctionWithErrors
      ).errors;
      return false;
    }
  } else {
    return false;
  }

  return true;
}

/**
 * This function validates the outputs of getBooking()
 */
export function validateGetBookingOutput(
  response: ResponseData,
): response is Signatures.GetBookingResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 200) {
    if (!BasicValidation.validateGetBookingResponse200Response(response.body)) {
      (validateGetBookingOutput as FunctionWithErrors).errors = (
        BasicValidation.validateGetBookingResponse200Response as FunctionWithErrors
      ).errors;
      return false;
    }

    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateGetBookingOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}

/**
 * This function validates the inputs to updateBooking()
 */
export function validateUpdateBookingInput(
  parameters: Signatures.UpdateBookingParameters,
  body: Signatures.UpdateBookingBody,
) {
  if (parameters) {
    if (!BasicValidation.validateBookingIdRequest(parameters['bookingId'])) {
      (validateUpdateBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateBookingIdRequest as FunctionWithErrors
      ).errors;
      return false;
    }
    if (
      !BasicValidation.validateChangedUrlRequest(parameters['changedUrl']) &&
      parameters['changedUrl'] !== undefined
    ) {
      (validateUpdateBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateChangedUrlRequest as FunctionWithErrors
      ).errors;
      return false;
    }
    if (
      !BasicValidation.validateDeletedUrlRequest(parameters['deletedUrl']) &&
      parameters['deletedUrl'] !== undefined
    ) {
      (validateUpdateBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateDeletedUrlRequest as FunctionWithErrors
      ).errors;
      return false;
    }
  } else {
    return false;
  }

  if (body === undefined) {
    return true;
  }

  if (!BasicValidation.validateUpdateBookingBodyRequest(body)) {
    (validateUpdateBookingInput as FunctionWithErrors).errors = (
      BasicValidation.validateUpdateBookingBodyRequest as FunctionWithErrors
    ).errors;
    return false;
  }

  return true;
}

/**
 * This function validates the outputs of updateBooking()
 */
export function validateUpdateBookingOutput(
  response: ResponseData,
): response is Signatures.UpdateBookingResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 200) {
    if (!BasicValidation.validateUpdateBookingResponse200Response(response.body)) {
      (validateUpdateBookingOutput as FunctionWithErrors).errors = (
        BasicValidation.validateUpdateBookingResponse200Response as FunctionWithErrors
      ).errors;
      return false;
    }

    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateUpdateBookingOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}

/**
 * This function validates the inputs to deleteBooking()
 */
export function validateDeleteBookingInput(
  parameters: Signatures.DeleteBookingParameters,
) {
  if (parameters) {
    if (!BasicValidation.validateBookingIdRequest(parameters['bookingId'])) {
      (validateDeleteBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateBookingIdRequest as FunctionWithErrors
      ).errors;
      return false;
    }
  } else {
    return false;
  }

  return true;
}

/**
 * This function validates the outputs of deleteBooking()
 */
export function validateDeleteBookingOutput(
  response: ResponseData,
): response is Signatures.DeleteBookingResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 204) {
    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateDeleteBookingOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}

/**
 * This function validates the inputs to lockBooking()
 */
export function validateLockBookingInput(parameters: Signatures.LockBookingParameters) {
  if (parameters) {
    if (!BasicValidation.validateBookingIdRequest(parameters['bookingId'])) {
      (validateLockBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateBookingIdRequest as FunctionWithErrors
      ).errors;
      return false;
    }
  } else {
    return false;
  }

  return true;
}

/**
 * This function validates the outputs of lockBooking()
 */
export function validateLockBookingOutput(
  response: ResponseData,
): response is Signatures.LockBookingResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 200) {
    if (!BasicValidation.validateLockBookingResponse200Response(response.body)) {
      (validateLockBookingOutput as FunctionWithErrors).errors = (
        BasicValidation.validateLockBookingResponse200Response as FunctionWithErrors
      ).errors;
      return false;
    }

    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateLockBookingOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}

/**
 * This function validates the inputs to unlockBooking()
 */
export function validateUnlockBookingInput(
  parameters: Signatures.UnlockBookingParameters,
) {
  if (parameters) {
    if (!BasicValidation.validateBookingIdRequest(parameters['bookingId'])) {
      (validateUnlockBookingInput as FunctionWithErrors).errors = (
        BasicValidation.validateBookingIdRequest as FunctionWithErrors
      ).errors;
      return false;
    }
  } else {
    return false;
  }

  return true;
}

/**
 * This function validates the outputs of unlockBooking()
 */
export function validateUnlockBookingOutput(
  response: ResponseData,
): response is Signatures.UnlockBookingResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 204) {
    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateUnlockBookingOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}

/**
 * This function validates the inputs to getSchedule()
 */
export function validateGetScheduleInput(body: Signatures.GetScheduleBody) {
  if (!BasicValidation.validateGetScheduleBodyRequest(body)) {
    (validateGetScheduleInput as FunctionWithErrors).errors = (
      BasicValidation.validateGetScheduleBodyRequest as FunctionWithErrors
    ).errors;
    return false;
  }

  return true;
}

/**
 * This function validates the outputs of getSchedule()
 */
export function validateGetScheduleOutput(
  response: ResponseData,
): response is Signatures.GetScheduleResponse {
  if (response.status < 100 || response.status >= 600) return false;

  if (response.status === 200) {
    if (!BasicValidation.validateGetScheduleResponse200Response(response.body)) {
      (validateGetScheduleOutput as FunctionWithErrors).errors = (
        BasicValidation.validateGetScheduleResponse200Response as FunctionWithErrors
      ).errors;
      return false;
    }

    return true;
  }

  if (response.status === 400) {
    return true;
  }

  if (response.status === 401) {
    return true;
  }

  if (response.status === 403) {
    return true;
  }

  if (response.status === 404) {
    return true;
  }

  if (response.status === 500) {
    return true;
  }

  (validateGetScheduleOutput as FunctionWithErrors).errors =
    `Response status ${response.status} is unexpected`;

  return false;
}
