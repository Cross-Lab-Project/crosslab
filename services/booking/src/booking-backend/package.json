{
  "name": "booking-backend",
  "version": "0.0.1",
  "description": "Part of booking service. Does most of the actual work and is horizontal scaleable.",
  "main": "index.ts",
  "scripts": {
    "test": "npm run build && npx nyc --include=\"build/messageDefinition.js\" --include=\"build/operations/internal.js\" mocha build/*_test.js",
    "build": "npx tsc",
    "start": "npm run build && node build/booking-backend/index.js"
  },
  "author": "Marcus Soll",
  "license": "Apache-2.0",
  "dependencies": {
    "@cross-lab-project/api-client": "^0.0.15",
    "@types/express": "^4.17.13",
    "dayjs": "^1.11.5",
    "express": "^4.18.1",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "mocha": "^10.0.0",
    "nyc": "^15.1.0"
  }
}
