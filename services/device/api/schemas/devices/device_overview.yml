# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
title: Device Overview
allOf: 
  - type: object
    properties:
      type:
        type: string
        description: Type of the device
        enum:
          - device
          - group
          - edge instantiable
          - cloud instantiable
    required:
      - type
  - $ref: ./device_overview_without_type.yml
x-typeguard: true
