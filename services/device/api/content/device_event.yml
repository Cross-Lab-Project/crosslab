# yaml-language-server: $schema=https://cross-lab-project.github.io/crosslab/meta-schemas/openapi-content.json
schema:
  type: object
  properties:
    callbackType:
      type: string
      enum:
        - event
    eventType:
      type: string
      enum:
        - device-changed
    device:
      anyOf:
        - $ref: "../schemas/devices/device_concrete.yml"
        - $ref: "../schemas/devices/device_group.yml"
        - $ref: '../schemas/devices/device_cloud_instantiable.yml'
        - $ref: '../schemas/devices/device_edge_instantiable.yml'
